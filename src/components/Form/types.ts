export interface IState {
  step: number;
}
export interface IFieldProps {
  name: string;
  label: string;
}
export interface IUser {
  first_name: string;
  last_name: string;
  city: string;
  age: string;
  is_active: boolean;
  id: number;
}

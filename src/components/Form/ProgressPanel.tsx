import { Grid, Typography } from "@mui/material";

function ProgressPanel() {
  return (
    <Grid sx={{ display: { xs: "none", md: "flex" } }}>
      <Typography>ProgressPanel</Typography>
    </Grid>
  );
}

export default ProgressPanel;
